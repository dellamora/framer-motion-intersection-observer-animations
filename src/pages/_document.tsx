import { Html, Head, Main, NextScript } from "next/document";

export default function Document() {
  return (
    <Html>
      <Head>
        <meta name="description" content="some cool animations with Framer Motion and Intersection Observer API in React" />
        <meta name="author" content="Francielle Dellamora"  />
      </Head>
      <body>
        <Main />
        <NextScript />
      </body>
    </Html>
  );
}
